# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增
- 全文索引移入后台线程（`SearchIndexWorker`）：建索引不再卡住主线程，期间 UI 可交互（A1）
- 性能基准 `bench/`：可复现的算法级耗时与"主线程阻塞"探针（A2）
- 崩溃恢复：未保存的编辑内容定时落草稿，异常退出后可找回（A3）
- 自研行级 diff（`LineDiff`）：不依赖 git 输出格式、返回结构化差异（B1）
- 大纲面板：标题树导航，围栏判据与语法高亮器共用同一份（C4）
- 焦点模式 + 写作统计：只突出当前段落；字数/词/段/句/阅读时长按需计算（C5）
- 版本历史膨胀率测量：`git gc` 可收回约 55% 且不丢版本，据此**不做** prune（C6）
- 主题导入导出：编辑器配色可存成 JSON 分享/导入，导入做 WCAG 对比度校验（C7）
- 版本对比可视化（`DiffView`）：行级红绿着色 + 行号两列，双击任一行跳回编辑器对应位置（#9）
- 崩溃恢复草稿接入主界面：15 秒定时落盘 + 切标签/关窗口/保存三处关键时机触发，启动时询问恢复（A3 收尾 / #16 关闭路径对称化）
- 块级解析缓存（`ParseCache`）：空行 + 围栏切块，用自研 diff 定位改动行、按块失效（#1）
- 预览增量刷新：缓存上次内容做"内容相同短路"，改动规模检测为段落级 patch 铺路（#2）
- 内容缓存升级：从"条数计数"改为"字节配额 + 按文件大小分三档"，新增淘汰字节/省读次数统计（#8）

### 修复
- 写作统计的中文句末标点判定：`QLatin1Char('。')` 会截断成半个字节导致句数恒为 0，改为按码点判断
- 内容缓存的字节统计：`QCache::object()` 会刷新 LRU 顺序，遍历统计改用只读的 `totalCost()`，修掉了淘汰顺序被统计动作搅乱的隐患

## [1.0.0] - 2026-09-13

### 新增
- 多标签、工作区式界面：左侧文件树、底部全文搜索、中间「编辑 + 实时预览」分屏
- 编辑增强：括号引号自动闭合、自动缩进、列表与引用回车续行
- 查找替换（Ctrl+F / Ctrl+H）：自动环绕、全部替换一次撤销、区分大小写
- md4c 渲染 + 双向同步（编辑区 ↔ 预览区）
- 代码高亮：31 种语言
- 亮色 / 暗色主题全局切换（不重载预览页面）
- 自动识别编码（UTF-8 / UTF-8 BOM / GBK / UTF-16）
- 本地版本历史（轻量快照 + 差异 + 回滚）
- 全文搜索（SQLite FTS5 行级索引）
- 导出 HTML（单文件）与 PDF（Chromium 打印管线）
- 最近打开、拖拽打开、窗口与会话记忆
- 大文档快速模式（30 万字符阈值）
- 23 个测试程序、1579 项检查

### 修复
- （v1.0.0 为第一个正式版本，无历史修复记录）

[Unreleased]: https://github.com/yylgxy/muse-md/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/yylgxy/muse-md/releases/tag/v1.0.0
